# کلید مدل — بدون این، موتور (vision/speech/generate) خطای ۵۰۳ برمی‌گرداند
OPENAI_API_KEY=

# اختیاری — نام مدل‌ها (پیش‌فرض‌ها معمولاً کافی‌اند)
CATALOGYAR_VISION_MODEL=gpt-4o-mini
CATALOGYAR_SPEECH_MODEL=whisper-1
CATALOGYAR_GENERATE_MODEL=gpt-4o-mini

# اختیاری — retry/backoff فراخوانی مدل (فقط روی خطای موقت: شبکه/۴۲۹/۵xx)
# تعداد کل تلاش‌ها؛ ۱ یعنی بدون retry
CATALOGYAR_MODEL_MAX_ATTEMPTS=3
# تأخیر پایه و سقف backoff نمایی (ثانیه)
CATALOGYAR_MODEL_RETRY_BASE_DELAY=0.5
CATALOGYAR_MODEL_RETRY_MAX_DELAY=8.0

# اختیاری — cache تحلیل تصویر بر اساس hash محتوا
# ۰ یعنی خاموش
CATALOGYAR_IMAGE_CACHE_ENABLED=1
# خالی بگذار تا پیش‌فرض (backend/.cache/image-analysis) استفاده شود
CATALOGYAR_IMAGE_CACHE_DIR=
# TTL ورودی‌های cache به ثانیه (پیش‌فرض: ۷ روز)
CATALOGYAR_IMAGE_CACHE_TTL_SECONDS=604800
